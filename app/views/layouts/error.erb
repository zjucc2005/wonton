<!DOCTYPE html>
<html lang="en">
    <head>
        <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
        <!--[if lt IE 9]>
        <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
        <![endif]-->
        <meta content="text/html; charset=utf-8" http-equiv="Content-Type" />
        <meta name=viewport content='width=device-width,initial-scale=1' />
        <title><%= @title.present? ? "#{@title} | Wonton Admin" : "#{pat(:custom_errors.title)} | Wonton Admin" %></title>
        <%= stylesheet_link_tag 'bootstrap', 'application' %>
    </head>
    <body>
        <div class="custom-error text-center">
            <div class="custom-error-body muted">
                <%= yield %>
            </div>
            <div class="custom-error-footer">
                <% link_to '#', :onclick => 'history.go(-1);return false;',
                            :class => 'custom-error-go-back btn btn-default pull-left', :title => 'Go back' do %>
                    <i class="glyphicon glyphicon-arrow-left"></i>
                <% end %>
            </div>
        </div>
    </body>
</html>