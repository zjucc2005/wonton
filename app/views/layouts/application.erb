<!DOCTYPE html>
<html lang="en" xmlns="http://www.w3.org/1999/xhtml">
    <head>
        <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
        <!--[if lt IE 9]>
        <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
        <![endif]-->
        <meta content="text/html; charset=utf-8" http-equiv="Content-Type">
        <meta name="viewpoint" content="width=device-width,initial-scale=1">
        <meta name="keywords" content="<%= @title %>">
        <meta name="description" content="<%= @title %>">
        <%= content_tag :title, @title.present? ? @title : 'HUIYUAN' %>
        <%= favicon_tag 'favicon-16x16.png' %>
        <%= stylesheet_link_tag 'bootstrap', 'zoomify', 'application' %>
        <%= javascript_include_tag 'jquery-1.7.0.min', 'bootstrap.min', 'zoomify.min', 'blowup.min' %>
    </head>

    <body>
        <% {:error => 'danger', :warning => 'warning', :success => 'success', :notice => 'info' }.each do |key, value| %>
            <%= flash_tag(key, :class => "alert alert-#{value} alert-dismissable fade in", :bootstrap => true) %>
        <% end %>
        <%= yield %>
        <%= javascript_include_tag 'application' %>
    </body>
</html>
