<ul class="nav nav-tabs">
    <li> <%= link_to tag_icon(:list, t(:list)), url(:products, :index) %></li>
    <li class="active"> <%= link_to tag_icon(:signal, t(:chart)), url(:products, :pv_rank) %></li>
    <li> <%= link_to tag_icon(:plus, t('action.new')), url(:products, :new) %></li>
</ul>

<div class="tabs-content">
    <div class="row">
        <div class="col-md-10 col-md-offset-1">
            <div id="chart_pv"  style="width: 100%; height:480px;">
            </div>
        </div>
    </div>
</div>

<script>
    (function(){
        var chart_pv = echarts.init(document.getElementById('chart_pv'), 'light');
        var option = {
            title: {
                text: '<%= t(:product_statistics) %>',
                subtext: 'BY PARTYALI'
            },
            legend: {},
            tooltip: {},
            dataset: {
                source: <%= @source.to_s.html_safe %>
            },
            grid: { containLabel: true },
            xAxis: { },
            yAxis: { type: 'category' },
            series: [
                {
                    type: 'bar',
                    label: {
                        normal: {
                            show: true,
                            position: 'insideRight'
                        }
                    }
                },
                {
                    type: 'bar',
                    label: {
                        normal: {
                            show: true,
                            position: 'insideRight'
                        }
                    }
                }
            ]
        };
        chart_pv.setOption(option);
    })()
</script>